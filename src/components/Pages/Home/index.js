import React from 'react';
import { Posts } from '../../Posts';
import { Layout } from '../../Pages';

export function Home() {
  return (
    <Layout>
      <Posts />
    </Layout>
  );
}
